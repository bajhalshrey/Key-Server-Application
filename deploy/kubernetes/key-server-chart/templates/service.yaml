apiVersion: v1
kind: Service
metadata:
  name: {{ include "key-server.fullname" . }}
  labels:
    {{- include "key-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: https
  selector:
    {{- include "key-server.selectorLabels" . | nindent 4 }}
